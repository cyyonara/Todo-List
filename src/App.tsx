import { FC } from "react"
import Box from "./components/Box";

const App : FC = () => {
    return (
    <div className="min-h-screen px-8 bg-blue-500 flex items-center justify-center">
      <Box />
    </div>
  )
}

export default App